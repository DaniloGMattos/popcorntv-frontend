import styled from "styled-components";

export const Container = styled.main`
  margin: 0 4rem;
  .grid {
    display: flex;
    gap: 2.5rem;
    flex-wrap: wrap;
  }
`;
export const Title = styled.h1`
  margin-top: 1rem;
`;
